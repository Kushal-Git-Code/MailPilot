# Progress Log — MailPilot

## Current Status
Last updated: 2026-08-26
Currently on: **Phase 8 (UI/UX Redesign & Polish) — in progress.** This session: `/dashboard` was substantially redesigned, not just polished — rows-only at-a-glance summary (Needs You + 4 category rows, each linking into `/dashboard/all` pre-filtered) replacing the old tile grid *and* the duplicate inline email list; `priority-list.tsx` and `session-summary.tsx` deleted entirely as a result. `/dashboard/all` got real color (tinted-at-rest/solid-when-active filter pills with counts, colored category badges, avatar circles, cleaned-up sender names), a fixed "Open in Gmail" deep link (was silently opening the wrong Google account for multi-account users), and a clearer category-correction control. Also: the classifier's priority rubric was tightened (routine account-confirmation emails were being over-flagged) and validated against `/evals` before shipping — see Phase 8 detail below. Settings pages and Steps 37-39 (contrast/mobile/empty-states) still remain.

## Completed
- [x] Phase 1: Project Setup (Steps 1-6) — `/web` and `/worker` scaffolded, `/shared` wired into both, `.env.example` documented, Supabase project created and connection verified
- [x] Phase 2: Database Schema (Steps 7-9) — complete
  - Step 7: migrations for all 5 tables (`users`, `gmail_tokens`, `emails`, `rules`, `audit_log`) written, applied, and verified against the live DB — including a real up/down/up rollback test, not just written and assumed correct
  - Step 8: RLS enabled + 20 policies (4 per table, `auth.uid()`-scoped) written in `0006_enable_rls.sql`, applied, and verified against live Postgres system catalogs. Caught and fixed a real bug: `CREATE POLICY` isn't idempotent, which broke a second `npm run migrate` run — fixed with `DROP POLICY IF EXISTS` before each `CREATE POLICY`. Rollback tested in isolation (single-file run, not full `migrate:down`, since that would also drop the tables from 0001-0005) — confirmed it disables RLS and removes all 20 policies without touching the tables, then reapplied to restore.
  - Step 9: seeded 1 row per table for two temporary auth users, verified cross-user RLS via real authenticated sessions (anon key + login, not the superuser DATABASE_URL connection, which bypasses RLS and can't actually test it) — User A could insert/see only their own rows across all 5 tables, an explicit query for User B's rows came back empty (not an error) on every table. Test users and rows cleaned up afterward (cascade delete), verified 0 rows remain in all 5 tables.
- [x] Phase 3: Authentication (Steps 11-14) — complete
  - Step 11: Supabase Auth (email + Google) implemented in `/web` — `lib/supabase/{client,server}.ts`, `middleware.ts` (protects every route except `/`, `/login`, `/auth/callback`), `/login` page (sign-in + sign-up + Google, styled per `docs/ui-ux-brief.md`), `/auth/callback` route, and a minimal `/dashboard` placeholder (real one is Phase 6) just to give the middleware something real to guard. You did the Google Cloud OAuth client + Supabase provider setup on your end.
  - Verified live in a real browser (not just build/typecheck): unauthenticated `/dashboard` → redirects to `/login`; sign-up with an invalid domain correctly errors, with a real domain correctly shows "check your email"; sign-in with a pre-confirmed test account → redirects to `/dashboard`, correct email shown; log out → redirects to `/`, and `/dashboard` is protected again; the Google button correctly initiates Supabase's OAuth `/authorize` redirect (didn't complete a real Google login — that needs your account, not something to automate). All test users cleaned up afterward.
  - Post-Step-11 fix: an automated security review caught an open redirect in `/auth/callback` (the `next=` query param was concatenated into a redirect URL unvalidated). Fixed by restricting it to same-origin relative paths only, falling back to `/dashboard` otherwise. Committed separately (`bae9e92`).
  - Step 12: separate Gmail-access OAuth (`gmail.readonly` + `gmail.modify`) implemented — `GET /api/auth/gmail/connect` (builds Google's consent URL with `access_type=offline`+`prompt=consent` so a refresh token is always issued, CSRF-protected via a random `state` in an httpOnly cookie) and `GET /api/auth/gmail/callback` (exchanges the code via `googleapis`, reads the connected address via `gmail.users.getProfile` rather than requesting an extra userinfo scope, encrypts both tokens with `lib/crypto.ts` (AES-256-GCM, `TOKEN_ENCRYPTION_KEY`), upserts into `gmail_tokens`). Minimal `/onboarding/connect` and `/onboarding/backlog` placeholders added to trigger/verify it (real ones are Step 15).
  - Caught and fixed a real bug during first real test: `gmail_tokens.user_id` FKs to `public.users`, but nothing was creating a `public.users` row on signup — only `auth.users` (Supabase's internal table) got one. Fixed with a new migration (`0007_sync_public_users_on_signup.sql`) adding an `AFTER INSERT ON auth.users` trigger that auto-creates the matching `public.users` row for every signup path (email, Google, admin-created). Backfilled the one user stuck in the broken state; verified the trigger fires correctly for a brand-new signup.
  - Verified end-to-end with your real Google account (`karter.t112@gmail.com`) — actually clicked through Google's real consent screen (not simulated), confirmed the resulting `gmail_tokens` row has the correct `gmail_address`/`status`, and confirmed both `access_token`/`refresh_token` are genuinely encrypted at rest (base64 ciphertext, not Google's real `ya29.`/`1//` formats) and correctly decrypt back to real, valid-looking Google tokens. All test users/rows cleaned up afterward, verified 0 rows remain in all 5 tables.
  - Note: your Google Cloud OAuth consent screen is in "Testing" mode — only accounts explicitly added under Test users can complete either OAuth client's flow (this project's, or Step 11's). `karter.t112@gmail.com` is now added; add any other real accounts you want to test with the same way. This is also the thing the TRD's "start Google verification review early" gotcha refers to — it gates going beyond ~100 test users, not something to rush now.
  - Step 13: `POST /api/gmail/disconnect` — decrypts the stored refresh token just long enough to call Google's actual revoke endpoint (`oauth2Client.revokeToken`), then sets `gmail_tokens.status = 'revoked'` (doesn't delete the row or null the now-inert encrypted tokens — schema doesn't call for that). Redirects to `/onboarding/connect?message=disconnected` with the "reconnect anytime, your data is safe" message per `docs/app-flow.md`. Trigger button added to the `/dashboard` placeholder (real Settings page is Phase 7).
  - Verified as rigorously as possible: reconnected Gmail with your real account again, clicked Disconnect, confirmed the DB flipped to `status: 'revoked'` — then went further and tried using the old (decrypted) access token directly against Google's API, which Google correctly rejected (`invalid authentication credentials`). Proves the token was genuinely revoked at Google's end, not just locally flagged.
  - **Step 14 done criteria met — Phase 3 complete.** Sign up, log in, connect Gmail, disconnect, and reconnect have all now been genuinely demonstrated end-to-end with a real Google account, not just individually unit-verified.
- [x] Phase 4: Core Feature — Backlog Onboarding & Date Range (Steps 15-17) — complete
  - Step 15: real `/onboarding/connect` and `/onboarding/backlog` pages built, replacing the Phase 3 placeholders. Mid-step, the design direction changed significantly (see below) — these are the first pages built under the new system.
  - **Design direction changed (2026-08-22):** at your request, `docs/ui-ux-brief.md` was rewritten from the original calm/restrained/single-accent direction to a cheerful, colorful, motion-forward direction — multiple accent colors (indigo primary `#6366F1`, coral secondary `#FB923C`, teal tertiary `#2DD4BF`), soft gradient background, tactile animated buttons (lift/tilt on hover, press on click) via `framer-motion` (newly installed), floating decorative blur "blobs." This applies going forward from Step 15; `/login` and the Step 11-13 placeholders still reflect the old direction and need revisiting to match (tracked below). One nice side effect confirmed in Step 16 testing: `/login` already picked up the new gradient/colors automatically, since it was built using semantic Tailwind tokens (`bg-accent`, etc.) rather than hardcoded old hex values.
  - Verified: typecheck + full production build both passed; visually confirmed live in browser — gradient/blobs/glow render correctly, backlog date-range selection highlights correctly, "Get Started" button animates in via `AnimatePresence`, confirmation state shows correctly.
  - Set up Redis on Railway for the first time — chose "Public Access" (TCP proxy) over the more secure CLI-tunnel option, for local-dev simplicity; **flagged to revisit before production (Phase 10)**, since it exposes Redis to the public internet (password-protected). `REDIS_URL` now holds a real resolved connection string (was previously an unused placeholder since Step 4). Connection verified directly (`PING`/`PONG`, `SET`/`GET` roundtrip) before building anything on top of it.
  - Step 16: `POST /api/onboarding/backlog` — validates the date range, confirms Gmail is actively connected, adds a job to a BullMQ queue (`lib/queue.ts`) backed by Redis. Queue name/job-data shape (`BACKLOG_QUEUE_NAME`, `BacklogJobData`) now live in `/shared` as the first *real* (non-type-only) shared export — required adding `transpilePackages: ["shared"]` to `next.config.mjs` and a `"main"` field to `shared/package.json`, since webpack needs actual resolvable JS for runtime imports, unlike the type-only imports used so far. `backlog-form.tsx` now calls this real endpoint instead of the earlier local-state-only placeholder.
  - **Step 17 done criteria met — Phase 4 complete.** Verified two ways: a direct `fetch()` call and a real UI button click (via `AnimatePresence`-driven "Get Started") both resulted in a genuine job landing in the Redis queue with correct `userId`/`dateRange`, confirmed by querying the queue directly (not just trusting the API's 200 response). Error paths (invalid range, Gmail not connected) also verified. Test data cleaned up (test user, queue jobs) afterward.
- [x] Phase 5: Core Feature — Classification Pipeline (Steps 18-24) — complete
  - Step 18: first real code in `/worker` since Step 2's hello-world. `worker/src/gmail/fetch.ts` — lists Gmail message IDs for a date range (`after:YYYY/MM/DD` query), fetches metadata (subject/from/snippet, via `format: "metadata"` — not full body) in bounded concurrent batches (10 at a time) with real exponential backoff on 429/403 rate-limit errors, per the hard rule in `CLAUDE.md`. `worker/src/jobs/backlog-worker.ts` — a BullMQ `Worker` (the consumer side; Step 16 only built the producer) that picks up queued jobs, decrypts the user's Gmail refresh token, and calls the fetch function.
  - Moved Gmail token encrypt/decrypt from `web/lib/crypto.ts` into `/shared` (`encrypt`/`decrypt`) — genuinely shared logic between `/web` (encrypts) and `/worker` (decrypts), better than risking two implementations drifting apart. `/web`'s two consumers updated to import from `shared` instead; `web/lib/crypto.ts` deleted.
  - Fixed an `ioredis` + `NodeNext` module resolution quirk in `/worker` (`import IORedis from "ioredis"` isn't constructable under strict NodeNext — use the named `import { Redis as IORedis } from "ioredis"` instead). Also added `"type": "module"` to `shared/package.json` to clear a Node runtime warning.
  - `worker/.env.local` created for the first time (Supabase service-role access, `REDIS_URL`, `TOKEN_ENCRYPTION_KEY`, Google credentials — mostly values already known from `/web`'s env, no new external setup).
  - **Verified with real data, not simulated:** ran `/web` and `/worker` dev servers together, connected your real Gmail account through the actual flow, triggered a real backlog job — the worker genuinely fetched **41 real messages** from the last 7 days of `karter.t112@gmail.com`, logged with no errors. Confirmed `emails` table stayed at 0 rows throughout (correctly matches Step 18's scope — fetch only, no persistence yet; that's Step 21). All test data cleaned up afterward.
  - Step 18 fetch function upgraded mid-Phase: switched from `format: "metadata"` to `format: "full"` (real MIME parsing — `text/plain` preferred, `text/html` stripped as fallback, capped at 3000 chars) after a deliberate discussion about classification accuracy — the PRD's "never miss something urgent" requirement needs more signal than Gmail's short auto-generated snippet gives, especially for ambiguous human messages. Confirmed this doesn't conflict with the zero-content-storage rule (still never written to Postgres, only sent transiently to Claude for one classification call). Full content does leave our infrastructure to a third party (Anthropic) — logged as a real trust-boundary distinction from "never stored," not glossed over. Compared against competitor "Tame My Inbox"'s actual privacy policy: confirmed this is standard practice in the product category (same reliance on Anthropic's no-training/limited-retention API terms), and confirmed MailPilot's zero-storage rule is *stricter* than Tame's real practice (they cache content server-side 24-48h; MailPilot never does) — kept the stricter rule deliberately.
  - Step 19: `worker/src/classification/classify.ts` — Claude Haiku 4.5 (`claude-haiku-4-5`, per `docs/trd.md`'s "cheapest model that hits the bar" rule), structured output via `client.messages.parse()` + `zodOutputFormat()` (not manual JSON parsing), Zod schema (`priority`, `category`, `reason`, `confidence`), default categories per the PRD (`Human`/`Notification`/`Newsletter`/`Transactional`/`Normal / Uncategorized`), system prompt biased toward `priority: true` when genuinely uncertain about a human message. `/evals/fixtures.json` — 26 fabricated labeled test emails (not real user data) covering all categories plus deliberately tricky edge cases (fake-urgency spam, low-stakes-but-human messages, automated-but-actionable OTP codes). `worker/src/classification/run-evals.ts` — runner reporting priority/category/combined accuracy, ready for Step 20's tuning pass. **Typecheck + full build verified; not yet run live** — blocked on a real `ANTHROPIC_API_KEY` (see Current Status above).
  - **Privacy policy gap logged as a new open item** (see below) after comparing against Tame My Inbox's actual policy — Google requires a "Limited Use Disclosure" section for sensitive Gmail scopes, and MailPilot has no privacy policy at all yet.
  - **Step 22 pulled forward** (independent of Claude, so buildable/testable while blocked on the API key): `worker/src/gmail/label.ts` — idempotent `MailPilot/Priority` label application. Gets-or-creates the label, checks the message's *current* label state before writing anything (label writes aren't idempotent by default — a real gotcha from `CLAUDE.md`), logs to `audit_log` *before* the Gmail write (per the hard rule), only ever adds this one label (never touches/removes anything else). Verified rigorously against a real Gmail account: first call applied correctly, second call correctly no-op'd (`alreadyLabeled: true`), label genuinely present in Gmail, exactly one `audit_log` row (not duplicated by the second call), and cleanup (label removed from message + label itself deleted) left zero trace on the real account afterward.
  - **Step 21 pulled forward too** (independent of Claude, same reasoning as Step 22): `worker/src/classification/persist.ts` writes classification results to `emails` + a corresponding `audit_log` entry (`action: "email_classified"`). While designing this, caught and fixed a real gap: `emails` had **no unique constraint on `(user_id, gmail_message_id)`** — a BullMQ job retry (network blip, worker crash mid-run) would have re-inserted duplicate rows for the same email, exactly the "Duplicate processing must be idempotent" edge case called out explicitly in the PRD. Fixed with migration `0008_emails_unique_per_message.sql` (wrapped in a `pg_constraint` existence check — Postgres has no `ADD CONSTRAINT IF NOT EXISTS`, same idempotency gotcha as the 0006 RLS migration, caught and fixed the same way before trusting it) + switched the insert to an `upsert(..., { onConflict: "user_id,gmail_message_id" })`. Verified: two calls with the same `gmail_message_id` return the same `emailId`, exactly 1 row exists (not 2), field values correct, and 2 `audit_log` entries (one per processing attempt — intentional, since each attempt is a real event worth logging even when the `emails` row itself was updated not duplicated). Pure DB-side test, no Gmail/browser needed. Cleaned up afterward, verified all tables empty.
  - **Step 20 complete (2026-08-23).** `ANTHROPIC_API_KEY` sorted (was briefly added to `web/.env.local` by mistake — moved to `worker/.env.local`, where classification actually runs). First real eval run: priority accuracy 92.3% (24/26), category 80.8% (21/26). Genuinely reviewed the failures rather than accepting the headline number — one was a real false negative worth fixing (an automated flight check-in reminder with a real deadline was marked non-priority; the original prompt only treated *human* messages as potentially time-sensitive), most of the category misses turned out to be my own fixture labels being arguably too strict rather than model errors (a calendar invite and a survey request are more naturally "Notification" than my original "Normal / Uncategorized"). Fixed the prompt (explicit deadline-based priority guidance; tighter `Transactional` vs `Notification` boundaries) and corrected those two fixture labels, then re-ran: **priority 96.2% (25/26), category 100% (26/26)**. The one remaining "failure" is a deliberate safe-direction over-flag (a low-stakes "lunch sometime?" message marked priority when it needn't be) — matches the PRD's explicit bias ("missing something urgent is worse than an extra flagged email"), not a defect. Validates the original Haiku 4.5 model choice — no need to escalate to a pricier model.
  - **Step 23: full pipeline wired together for the first time.** Until now, `backlog-worker.ts` only fetched messages — classify/persist/label existed as separate tested-in-isolation functions. Step 23 connected them: fetch → skip already-processed (via Step 21's unique constraint) → classify → persist → label, all inside one job. `worker/src/limits/dailyCap.ts` — rolling 24h window (not calendar-day-aligned) against `users.daily_email_count`/`daily_count_reset_at`. When a job hits the cap mid-scan, the leftover messages aren't dropped — the same job re-enqueues itself with a delay until the next reset (`worker/src/jobs/queue.ts`, a producer instance the worker needed for the first time, alongside its existing consumer).
  - **Verified with three real, deliberate tests** (not just a smoke test) against a real Gmail account + real Claude, per your explicit ask for certainty:
    - **Test A (full pipeline, no cap):** 39 real emails fetched → all classified → 13 flagged priority. Independently confirmed via direct DB queries (39 `emails` rows, 13 `priority_flagged=true`, `daily_email_count=39`, 39 `email_classified` + 13 `label_applied` audit rows — all matching exactly) *and* by querying Gmail directly for messages actually carrying the label (13, matching).
    - **Test B (idempotency):** re-ran the identical job — 0 messages processed, 0 flagged, DB state completely unchanged (same 39/13/39 counts). Confirms retries can't double-process or double-label.
    - **Test C (cap enforcement):** manually set `daily_email_count=73` (2 remaining), ran a wider 30-day scan — found 120 total messages, correctly skipped the 39 already done, correctly processed only 2 of the 81 new ones, correctly left 79 for later. Confirmed a real delayed job exists in the Redis queue with the correct `userId`/`dateRange` and a ~24h delay (1439 min). `emails` row count and `daily_email_count` both landed exactly where expected (41, 75).
    - All test data (test user, Gmail label + its removal from all 15 labeled messages, Redis queue) cleaned up afterward — verified zero trace left in the real Gmail account and all 5 tables empty.
  - **Step 24 done criteria met — Phase 5 complete.** Connecting a real Gmail account and picking a date range now genuinely results in correctly labeled emails in Gmail and correct rows in `emails`/`audit_log`, with zero email content ever written to Postgres — demonstrated end-to-end, not just assumed from the individual pieces.
- [x] Phase 6: Core Feature — Dashboard (Steps 25-30) — complete
  - **Step 25 done (2026-08-23): real `/dashboard` page**, replacing the Step 11 placeholder — first page built under the new colorful/motion-forward design system to combine real data with real Framer Motion polish (earlier pages were either data-free placeholders or forms).
    - `web/lib/gmail.ts` — `getGmailClientForUser(userId)`: loads the user's `gmail_tokens` row, decrypts the refresh token (via `shared`'s `decrypt`), returns a ready `googleapis` Gmail client, or `null` if not connected/revoked.
    - `web/lib/gmailDisplay.ts` — `getEmailDisplayInfo(gmail, messageIds)`: fetches live sender/subject (Subject/From headers only, `format: "metadata"`) for a batch of message IDs, batched at concurrency 10 (mirrors the fetch pattern from `worker/src/gmail/fetch.ts`), backed by a **module-scoped in-memory `Map`** (never Redis/Postgres/disk) — this *is* the TRD's "finalized" batched-fetch-plus-in-memory-cache performance strategy, satisfying Step 26's goal as a side effect of building Step 25 correctly; Step 26 in Phase 6 is really just formalizing/verifying this rather than new work.
    - `web/app/dashboard/priority-list.tsx` — client component rendering the flagged list with staggered spring-animated `motion.li` entries (Framer Motion), each linking out to the real Gmail thread in a new tab (`mail.google.com/mail/u/0/#all/{threadId}`) — dashboard never embeds email content itself, consistent with the zero-content-storage rule.
    - `web/app/dashboard/page.tsx` — rewritten server component: loads the user's flagged (`priority_flagged=true`) `emails` rows from Postgres (metadata only — id, `gmail_message_id`, `gmail_thread_id`, `classification_reason`, `received_at`), calls `getEmailDisplayInfo` for live sender/subject, renders either a "Gmail not connected" empty state, a "You're all caught up" empty state, or the populated `PriorityList`. Retains the account controls (Disconnect Gmail, Log out) from the old placeholder.
    - **Verified live, real data, three states, not just typecheck:** (1) "Gmail not connected" empty state confirmed by screenshot before connecting; (2) "You're all caught up" empty state confirmed by screenshot after connecting but before any classification job ran; (3) populated state confirmed after a real 7-day backlog job classified 42 real emails (14 flagged) — screenshot showed "Review Priority (14)" with correct real sender names/addresses, subjects, AI-generated reasons, and dates, styled per the design system, scrolling correctly through the full list.
    - **Caught and fixed a real hydration bug during verification**, not assumed away as a browser quirk: a red "1 error" toast appeared during live testing. Investigated properly via `read_console_messages` rather than guessing — found a genuine React hydration mismatch (`Text content did not match. Server: "23 Aug" Client: "Aug 23"`) in `priority-list.tsx`'s date formatting. Root cause: `toLocaleDateString(undefined, ...)` lets the locale resolve independently on the server (Node process locale) and the client (browser locale), which can disagree on month/day ordering for the identical date. Fixed by pinning an explicit `"en-US"` locale so server and client always agree. Re-verified after the fix: fresh page load and a full scroll through all 14 items produced zero console errors.
    - All Step 25 test artifacts cleaned up: test user `mailpilot-step25-test@mailinator.com` deleted (cascade-verified all 5 tables at 0 rows), the real `MailPilot/Priority` Gmail label removed from all 14 labeled messages and deleted from the account itself, Redis backlog-classification queue keys cleared, throwaway scripts (`worker/src/test-enqueue.ts`, `worker/src/test-cleanup-step25.ts`, `web/scripts/make-test-user.mjs`) deleted. (`web/scripts/show-encryption-demo.mjs` intentionally kept, per earlier instruction — not part of this cleanup.)
    - **Flagged, not fixed (at the time):** classification runs sequentially (one Claude call at a time), so a ~40-email backlog job takes roughly 40-80 seconds. Noted when you asked why it felt slow. **Fixed in Step 27 below.**
  - **Step 26 done — confirmed already satisfied by Step 25.** `web/lib/gmailDisplay.ts`'s batched (concurrency 10 at the time) live Gmail fetch + module-scoped in-memory `Map` cache, built as part of Step 25, *is* this step's deliverable. No separate work needed; just formally marking it complete.
  - **Step 27 done (2026-08-23): `/dashboard/all` secondary category view**, plus two real speed fixes and one real bug found during your own review of the live app.
    - **Default category display names** (worked out earlier this session, recorded in `docs/app-flow.md`): Needs You / Primary / Alerts / Newsletters / Transactional / Other. Display-layer only — the AI's actual classification categories are untouched.
    - `web/app/dashboard/all/page.tsx` + `all-list.tsx` — one unified list (not a multi-panel dashboard, per `docs/ui-ux-brief.md`'s "no dense multi-color category dashboards" constraint), with a lightweight pill-filter row (All / Needs You / Primary / Alerts / Newsletters / Transactional / Other) filtering client-side on data already fetched — no server round-trip per filter click.
    - **Real bug found by you during live review, not by me:** under any category tab (e.g. "Alerts"), items that were *also* `priority_flagged: true` showed a "Needs You" badge instead of the tab's own label — technically correct filtering (the item genuinely matches that category), but the badge contradicted the tab you were looking at. Root cause: the badge function always picked "Needs You" first whenever priority was true, regardless of which tab was active. Fixed by making the badge reflect the *active filter* when one is selected (all items under "Alerts" now say "Alerts"), falling back to the priority-first label only under "All". Verified across every tab afterward: Needs You (27/27), Newsletters (35/35), Transactional (7/7) all showed one consistent, correct badge each — not just the one tab you happened to screenshot.
    - **Speed fix 1 — classification concurrency.** `worker/src/jobs/backlog-worker.ts` batched to 10 concurrent Claude calls per round (same pattern as `gmail/fetch.ts`), replacing the sequential one-at-a-time loop. Verified with a real 50-message job (the leftover from a 30-day/125-message backlog scan, released by manually resetting the test user's daily cap the same way Step 23's Test C did): **~38 seconds total** (fetch + classify) versus an estimated ~155-165s the old sequential code would have taken for the same job — roughly a 4x improvement. Correctness cross-checked afterward: 125 total emails, 125 unique `gmail_message_id`s (no duplicates from concurrent writes), 41 flagged priority in the DB matching exactly 41 real Gmail messages carrying the label.
    - **Speed fix 2 — `/dashboard/all` scaling.** The live Gmail metadata fetch (sender/subject) has no natural size ceiling the way the priority-only dashboard does, so it would keep getting slower as an account's classified history grows. Fixed two ways: raised `web/lib/gmailDisplay.ts`'s fetch concurrency from 10 to 20 (Gmail's per-user quota comfortably allows it), and added the backoff-on-429/403 protection it was missing (a real gap, now closed, matching `CLAUDE.md`'s "always treat Gmail rate limits as a hard constraint" rule) — plus capped the `/dashboard/all` query at the 150 most-recent emails so worst-case cost stays bounded regardless of total history, with a visible "Showing your most recent 150" note so the cap is never silently misleading. Full pagination isn't built; this is a deliberate scope boundary for now, not an oversight.
    - All test artifacts cleaned up: test user `mailpilot-step27-test@mailinator.com` deleted (all 5 tables verified at 0 rows), the real `MailPilot/Priority` label removed from all 41 labeled messages and deleted from the account, Redis queue cleared, throwaway scripts deleted, dev servers stopped (again including one orphaned `node.exe` on port 3000 — the same recurring Windows `TaskStop` gotcha, caught and killed).
  - **Step 28 done (2026-08-23): manual correction control (US-3) — the "smarter" version, not just a one-off fix.** Discussed the scope directly with you first: the PRD's actual AC for US-3 says a correction should be "referenced in future prompts as a per-user preference signal," not just fix the one email. You chose that fuller version.
    - **New hard-rule exception, written into `CLAUDE.md` first, not silently done:** a corrected email's sender address may now be stored — the one narrow, user-triggered exception to "never store sender," scoped strictly to corrections (never for display, never for uncorrected emails). Chose the real address over a hashed one after walking through the tradeoff with you directly.
    - `shared/src/label.ts` (new) — `applyPriorityLabel` moved here from `worker/src/gmail/label.ts` (deleted), plus a new `removePriorityLabel`. Needed in both `/worker` (initial classification) and `/web` (corrections) now, so it's shared rather than duplicated — same reasoning as the encrypt/decrypt move in Step 18. Required adding `googleapis` + `@supabase/supabase-js` as real dependencies of `shared` for the first time (previously dependency-free), and a `resolve.extensionAlias` fix in `web/next.config.mjs` — webpack doesn't auto-map a `.js` import specifier to a `.ts` file the way `tsc`'s "bundler" resolution does, so `shared`'s internal `./label.js` import broke the production build until this was added.
    - `web/app/api/emails/[id]/correct/route.ts` (new) — flips `priority_flagged`, sets `user_corrected = true`, applies/removes the real Gmail label to match, logs `audit_log` (`action: correction_made`), and live-fetches the sender (only now, only because this is an explicit correction) to write a `rules` row (`rule_type: correction_signal`).
    - `worker/src/classification/applyCorrectionOverride.ts` (new) — a remembered per-sender correction always overrides the AI's own priority verdict for future emails from that sender (category/reason still come from the model). Wired into `backlog-worker.ts` via a `loadSenderCorrectionRules` lookup before each batch.
    - **Real bug caught during verification, not in the new code itself — a regression from Step 27's concurrency fix:** with 10 concurrent `applyPriorityLabel` calls hitting a brand-new account with no `MailPilot/Priority` label yet, multiple calls raced to create the label simultaneously — Gmail's API only lets one creation succeed, the rest failed with "Label name exists or conflicts," crashing the whole job. Never surfaced before because every prior test account already had the label from earlier runs. Fixed by making `getOrCreatePriorityLabelId` self-heal on a 409/400 conflict (re-fetch and use the now-existing label instead of failing) — a shared-module fix, since this could race across `/worker` and `/web` too, not just within one job's own batches.
    - **Verified rigorously, each piece separately:**
      - The override logic itself: tested directly against `applyCorrectionOverride` (the real production function, not a re-implemented copy) with real Claude calls — a genuinely urgent human message (AI said `priority: true`) forced to `false` by a rule, a genuinely routine automated message (AI said `false`) forced to `true` by a rule, and an unrelated sender with no rule passing through unchanged. All three passed.
      - The end-to-end correction flow: three real corrections against real Gmail (two "not priority," one "mark as priority") — each cross-checked across the `emails` row (`priority_flagged`/`user_corrected`), the `audit_log` entry (correct `previous_state`/`new_state`), the `rules` row (correct sender + priority), and the *actual* Gmail message's real labels (confirmed via direct API query) — the label genuinely appeared/disappeared, nothing else on the message was touched.
      - Timing, per your standing preference for concrete numbers: the very first correction took 21.5s — a one-time dev-mode cold start (first-ever load of `googleapis` in this route), not a per-request cost. The second and third corrections took 3.4s and 3.6s — the real, repeatable per-click cost (Gmail label check/write + a handful of sequential DB writes).
    - All test artifacts cleaned up: test user `mailpilot-step28-test@mailinator.com` deleted (all 5 tables, including `rules` for the first time, verified at 0 rows), the real label removed from all 13 labeled messages and deleted, Redis cleared, throwaway scripts deleted, dev servers stopped (yet again including an orphaned `node.exe` on port 3000).
  - **Step 29 done (2026-08-23): undo (US-5) — a pure safety-net revert, distinct from correction.** Correction (Step 28) says "this is wrong, fix it and remember"; undo says "put it back exactly, teach nothing." Reverses whatever the *most recent* `audit_log` entry for an email actually was, whatever kind it was — not a fixed "flip the flag" action.
    - `web/app/api/emails/[id]/undo/route.ts` (new) — looks at the latest `audit_log` row (must still be within its `reversible_until` window), and reverses it by action type: `label_applied` → remove the label; `label_removed` → reapply it; `correction_made` → revert `priority_flagged` *and* deactivate the specific `rules` row that correction created (found via a `ruleId` now carried inside the `correction_made` audit entry's `new_state`, added specifically to make this precise instead of guessing) *and* reset `user_corrected` back to `false`. Every undo itself is logged as a new `action: undone` entry, so a second undo click reverses the undo too — a natural, unforced consequence of "always reverse the most recent entry," not special-cased.
    - `web/lib/undoability.ts` (new) — one batched query per page load (not one per email) fetching each displayed email's latest still-reversible `audit_log` entry, used to decide whether to show the "Undo" link at all. Wired into both `/dashboard` and `/dashboard/all`.
    - **Real bug hunted down during verification — cost real time, turned out to be my own test mistake, not application code.** The Undo button appeared to silently not render for one specific corrected email, even after direct server-side debug logging proved the data reaching the client was correct (`"undoable":true` present in the raw RSC response itself, confirmed via a `cache: 'no-store'` fetch of the live page). Root cause, found only after inspecting *every* rendered item's debug marker side by side: the test's own sender-substring matching (`"emergent.sh"`) collided with a *different* email from the same sender still sitting further down the list — the actually-corrected email was rendering perfectly the whole time. No product bug existed. Kept in here because the investigation itself surfaced something worth keeping: a genuine Next.js App Router gotcha (Server Component responses can be cached unless a route explicitly opts out) was fixed as a byproduct — added `export const dynamic = "force-dynamic"` to both `/dashboard` and `/dashboard/all`, since their data can change between visits and neither should ever risk serving a stale cached response, independent of whatever caused this specific false alarm.
    - **Verified rigorously, both reversal paths, against real Gmail:**
      - Undoing a plain `label_applied`: real label genuinely removed from the real message (confirmed via direct Gmail API query, not just the DB), `priority_flagged` flipped to `false`, full audit trail correct (`email_classified` → `label_applied` → `undone`, with `undone.new_state.undidActionId` correctly pointing at the entry it reversed).
      - Undoing a `correction_made`: `priority_flagged` reverted to its pre-correction value, `user_corrected` reset to `false`, the specific `rules` row the correction created flipped to `active: false` (verified by its own id, not just "some rule"), and the real Gmail label reapplied — confirmed present again via direct API query.
    - All test artifacts cleaned up: test user `mailpilot-step29-test@mailinator.com` deleted (all 5 tables verified at 0 rows), the real label removed from all 13 labeled messages and deleted, Redis cleared, throwaway scripts deleted, dev servers stopped (the same recurring orphaned `node.exe` on port 3000, caught and killed again).
  - **Step 30 done-criteria check (2026-08-23): found real gaps against the PRD, not just re-confirmed what individual steps already claimed.** Checked each of US-1 through US-5's actual acceptance criteria (not just each step's own narrower wording) against what's genuinely built: **US-1 ✅, US-5 ✅ fully solid. US-2, US-3, US-4 each had a real gap** — classification/labeling work, but there's no ongoing polling for new mail (US-2's "within 2 minutes of arrival" AC); only priority was correctable, not category (US-3's "priority/category" AC); and no daily summary existed at all (US-4). Discussed each honestly with you rather than silently declaring the step done or silently fixing scope without asking. You chose to close Gap 2 (category correction) and Gap 3 (daily summary) now, and hold Gap 1 (ongoing polling) as its own deliberate, carefully-tested piece of work — it's the first feature in the whole project that runs continuously in the background rather than being user-triggered, a real shift in risk profile that shouldn't be rushed alongside the other two.
    - **New standing bar, applied from here on:** every code change explicitly checked for robustness, best practices, security, bug-freedom, and performance — not just "does it work once."
    - **Gap 2 closed — category correction.** Extended `POST /api/emails/[id]/correct` to accept `category` independently from `priority` (either or both). Category corrections never touch Gmail at all (category is dashboard-only, per the existing hard rule) — simpler and lower-risk than priority corrections by construction. Uses its own rule type (`category_correction_signal`, separate from priority's `correction_signal`) specifically so correcting one dimension for a sender can never clobber the other — a real design risk that would've existed if they'd shared one rule row. `worker/src/classification/applyCorrectionOverride.ts` gained a sibling `applyCategoryOverride` function (kept separate from the priority override, not merged, for single-responsibility) and its own `loadSenderCategoryRules` lookup in `backlog-worker.ts`. `DEFAULT_CATEGORIES` moved from `worker/src/classification/classify.ts` into `shared` so the web route can validate a submitted category against the exact same values the classifier is allowed to output — no drift possible. A category picker (`web/app/dashboard/category-correction-select.tsx`) was deliberately kept as a client-safe file with its own hardcoded category/display-name constants rather than importing `DEFAULT_CATEGORIES` from `shared` — that package's barrel file also re-exports `googleapis`/`node:crypto`-dependent code, which would have bloated or broken the browser bundle if pulled into a client component. Confirmed via the production build (`/dashboard/all`'s bundle grew by ~190 bytes, not the tens of kilobytes a leaked server SDK would cause).
      - **Real bug found and fixed during verification, not shipped silently:** after undoing a category correction, `user_corrected` stayed `true` instead of resetting to `false` — an inconsistency with how the priority-undo path already behaves in the equivalent situation. Fixed to match.
      - Verified rigorously: the override logic directly (real Claude call, AI said `Newsletter`, a rule forced `Transactional`, confirmed the override — not a re-implemented test copy); the full correction + undo round-trip against real data (`emails.category`, the `category_corrected` audit entry with its `ruleId`, the `rules` row, then undo reversing all three including the `user_corrected` fix); and a regression pass on the *existing* priority correction + undo path (unchanged logic, still confirmed against real Gmail labels) since both route files were substantially rewritten this step.
    - **Gap 3 closed — session-based triage summary** (deliberately *not* a rolling 24h window — see below). New `triage_sessions` table (migration `0009`, RLS enabled in the same migration that creates the table this time, not a follow-up one — no window where a new table sits unprotected). One row written by `backlog-worker.ts` per completed job run — chosen over reconstructing "session" boundaries from `audit_log` timestamp-clustering, which would have been fragile against Step 27's concurrent-batch timestamps landing in bursts. `web/app/dashboard/session-summary.tsx` reads the latest session and shows "N triaged · N flagged · N no action needed — Last scan: [time]".
      - **Framing decided by directly comparing against Tame My Inbox's actual product** (their screenshot shows "Session from [date] at [time]" with counts, not a rolling window) — session-based matches how MailPilot genuinely behaves today (manually-triggered scans only). Explicitly deferred to revisit as a rolling-24h window once Gap 1 (ongoing polling) exists and "daily" becomes literally true rather than "since your last manual scan."
      - **"Auto-archived" from the PRD's literal AC deliberately left out** — MailPilot has never archived anything, only labeled. Reframed around what the app actually does (no action needed) rather than fabricate a number for a feature that doesn't exist. Whether to build real auto-archiving is an open decision, explicitly deferred until later.
      - Verified against real data: `50 triaged · 14 flagged · 36 no action needed` rendered correctly, math checked (14+36=50), timestamp formatted correctly (explicit `"en-US"` locale — the Step 25 hydration-bug lesson applied proactively here, not re-learned).
    - All test artifacts cleaned up: test user `mailpilot-gap23-test@mailinator.com` deleted (all **6** tables now, including `triage_sessions` for the first time, verified at 0 rows), the real label removed from all 13 labeled messages and deleted, Redis queue keys cleared (two benign BullMQ internal bookkeeping keys — `:meta`, `:stalled-check` — regenerate automatically and hold no job/user data, not a cleanup gap), dev servers stopped (the same recurring orphaned `node.exe` on port 3000, caught and killed again).
  - **Gap 1 closed (2026-08-24) — ongoing polling for new mail (US-2).** Discussed cost/tradeoffs with you directly before building: a 2-minute interval (Tame's/IRIS's actual cadence isn't publicly documented, so this wasn't a like-for-like match) would burn Claude tokens continuously for every connected user regardless of whether new mail ever arrives, for a use case where email itself already tolerates real latency. Landed on your brainstormed hybrid — **hourly automatic polling plus a manual "Check now" button** on the dashboard — over the original 2-minute design.
    - **Architecture:** a new, separate `poll-scheduler` BullMQ queue whose only job is fan-out — on each hourly tick, find every user with an active Gmail connection and enqueue a per-user "forward" scan onto the *existing*, unchanged classification queue/pipeline. Kept deliberately separate from the classification work itself (`worker/src/jobs/pollQueue.ts` producer, `pollScheduler.ts` consumer + `scheduleRecurringPoll()`, registered on worker boot in `worker/src/index.ts`).
    - Recurring scheduling uses BullMQ's `Queue.upsertJobScheduler(jobSchedulerId, repeatOpts, jobTemplate)` — **not** `Queue.add(name, data, {repeat: {...}})`, which no longer exists as of BullMQ 6.2.0 (`repeat` was removed from `JobsOptions` entirely). Caught via a real `TS2353` typecheck failure, root-caused by reading the actual installed package's `.d.ts` files rather than trusting a stale training-data assumption about the API shape. `upsertJobScheduler` is idempotent-by-design (safe to call on every worker restart) and fires an immediate first tick on registration.
    - **`worker/src/gmail/fetch.ts` restructured** (split into `listGmailMessageRefs` + `fetchFullMessages`, plus `forwardSinceDate` support) so that listing message IDs (cheap) happens before fetching full body content (expensive) — a real performance requirement for polling specifically: without this split, an hourly poll of a mostly-already-processed inbox would re-download full bodies for old mail every single cycle. Also benefits the existing manual-scan flows, not just Gap 1.
    - **The "forward" cursor**: Gmail's `after:` search is date-only, not minute-precise, so this is a coarse starting point, not a precise one — exact new-vs-seen precision still comes from the existing `alreadyProcessedIds` DB check (Step 21's unique constraint), same as every other scan type. `resolveForwardSinceDate()` in `backlog-worker.ts` uses the most recent `triage_sessions.completed_at` when one exists, falling back to the user's `gmail_tokens.connected_at` for a brand-new "forward-only" signup with no session yet.
    - **Real bug found and fixed during live verification, not shipped silently:** the per-user forward job uses a static, reused `jobId` (`forward-${userId}`) so the scheduler can recognize an in-flight scan and skip re-enqueueing a duplicate. It was created with `removeOnComplete: { count: 50 }` / `removeOnFail: { count: 100 }`, on the assumption that would free the id for reuse once each run finished. **It doesn't.** BullMQ's `addStandardJob` Lua script checks `EXISTS jobIdKey` before doing anything else — if a job with that id is still present in Redis (regardless of state: completed, failed, active), `queue.add()` silently returns the *existing* job instead of creating a new one (`handleDuplicatedJob`, confirmed by reading the actual Lua source, not assumed). A count-based retention policy only evicts once *more than N* completions pile up under an id — which structurally never happens for a single reused static id, since there's only ever one job under it at a time. Net effect: the very first poll tick for a user would work, and **every poll tick after that would silently no-op forever**, with the scheduler's own log line (`"1 enqueued"`) misleadingly implying success, since `add()` not throwing was the only thing being checked.
      - Caught by directly inspecting the job's Redis state after firing two consecutive manual ticks against the real test account: `attemptsMade` stayed at `1` across both ticks — proof the second tick's "enqueue" never actually ran anything.
      - Fixed by switching both `removeOnComplete`/`removeOnFail` to `true` (not a count) in both `pollScheduler.ts`'s fan-out enqueue and `backlog-worker.ts`'s daily-cap continuation re-enqueue — this removes the job from Redis the instant it completes/fails, genuinely freeing the static id for the next tick. Job history for this internal queue isn't needed for debugging (`triage_sessions`/`audit_log` are the real sources of truth for what happened), so losing retention here costs nothing.
      - Re-verified after the fix, cleanly (removed the stale pre-fix job first, so the test wasn't contaminated by data created under the old broken options): two consecutive manual ticks against the real connected test account both produced a genuine new `[backlog-worker] Processing job forward-...` run each time. Separately verified the scheduler's in-flight dedup skip is still correct by planting a decoy delayed job under the same static id and confirming a tick correctly logged `"0 enqueued, 1 skipped (already in flight)"` rather than double-enqueueing.
    - **Verified end-to-end against real Gmail** (`karter.t112@gmail.com`, fresh test signup choosing "From today forward only" at onboarding): correctly found only messages received that day (11 refs, via the `connected_at` fallback since no prior session existed), classified all 11, flagged 1 priority, wrote a `triage_sessions` row, and the dashboard's session summary rendered "11 triaged · 1 flagged · 10 no action needed" correctly. Also confirmed the scheduler genuinely picks this up on its own an hour later with no manual trigger at all — the worker log showed a real, unprompted `[poll-scheduler] Checked 1 user(s): 1 enqueued` tick roughly an hour after the previous one, which found and correctly processed 1 newly-arrived message.
    - **Regression-checked the existing correction/undo paths**, since both `fetch.ts` and `backlog-worker.ts` were substantially rewritten this step: a real "Not priority" correction against the live flagged email completed successfully (`POST /api/emails/.../correct` → `200`, ~59s — the same one-time dev-mode cold-start cost documented back in Step 28, not a new regression), and the resulting `rules` row (`correction_signal`, correct sender + `priority: false`) confirmed the override logic is intact.
      - Note for next time this comes up: while regression-testing, browser-automation clicks (both `find`-ref and coordinate-based) on the correction button repeatedly produced *no* network request at all, even though the button's own `onClick` handler was confirmed correctly wired (a direct DOM `.click()` via `javascript_tool` worked immediately). This matches the already-documented gotcha at the bottom of this file about `find`+`computer` clicks not reliably reaching React handlers — not a new issue, but cost real time before connecting it to the existing note. If a click via the browser tool doesn't seem to register, don't chase it as a product bug — go straight to `javascript_tool` + direct `.click()`.
    - All test artifacts cleaned up: test user `mailpilot-gap1-test@mailinator.com` deleted (Auth user removed via `admin.auth.admin.deleteUser`, all **6** Postgres tables cascade-verified at 0 rows), the real `MailPilot/Priority` label removed from the one still-labeled message and deleted from the account, both BullMQ queues (`backlog-classification` and `poll-scheduler`) obliterated to clear all job/scheduler history, throwaway scripts deleted (`web/scripts/make-test-user.mjs` + its one-off deletion counterpart, and the whole `worker/scripts/` directory created for this step's Redis/Gmail diagnostics).
  - **Step 30 done criteria now genuinely met — Phase 6 complete.** All of US-1 through US-5 demonstrated end-to-end against real Gmail this session, not just individually step-verified: mail gets triaged and labeled (US-1), new mail is now caught automatically within an hour without any manual trigger plus an on-demand "Check now" escape hatch (US-2), both priority and category are correctable per-sender (US-3), a session summary is visible on the dashboard (US-4), and any single action can be undone (US-5).
- [x] Phase 7: Core Feature — Settings (Steps 31-35) — complete
  - **Steps 31, 33, 34 done (2026-08-24).** Built in that order deliberately — the two contained, low-risk pages first, then the bigger structural nav change, leaving Step 32 (custom categories, which touches the classification prompt + `/evals`) for last.
  - Step 31: `/settings/account` — reads `gmail_tokens.gmail_address`/`status` directly (no OAuth client needed for a display-only page), reuses the existing `/api/gmail/disconnect` route from Step 13 unchanged.
  - Step 33: `/settings/plan` — `DAILY_CAP` moved from `worker/src/limits/dailyCap.ts` into `/shared` so `/web` can display the exact same number `/worker` enforces, never a second hardcoded copy that could drift. Deliberately **read-only**: mirrors the worker's rolling-24h reset logic for display but never performs the reset itself — a settings page render shouldn't have the side effect of mutating usage state (a GET should stay a GET); the worker still does the real lazy reset on its next job, unchanged.
  - Step 34: full sidebar nav, built as a Next.js route group (`app/(app)/`) wrapping `/dashboard`, `/dashboard/all`, and both new settings pages in one shared layout — route groups add no URL segment, so every existing path stayed exactly the same. Inbox and Settings are real active links (highlighted via `usePathname` prefix match); Commitments and Unsubscribe render visually muted with a "Soon" tag, per `docs/app-flow.md`'s nav spec — not built, not clickable. Styled with today's existing indigo/coral/teal tokens on purpose, not the Quell-inspired direction — this shell gets restyled in Phase 8, not rebuilt.
    - **Real Windows gotcha hit and fixed:** moving `dashboard/` and `settings/` into the new route group via `mv`/`git mv` failed with "Permission denied" — the running `next dev` process was holding file handles open on those directories (same class of issue as the recurring orphaned-`node.exe`-on-port-3000 gotcha, just blocking a file move instead of leaving a stale port). Fixed by stopping the dev server first, then moving cleanly.
    - Removed a redundant duplicate "Log out" button that the two new settings pages had in their own headers, now that the sidebar has one at the bottom — caught during live verification, not shipped. Added a small `SettingsTabs` cross-link component (Account | Plan & usage) so the two settings pages can navigate between each other without needing sidebar sub-menu UI for Categories (Step 32), which doesn't exist yet.
    - Verified live end-to-end: fresh test signup, sidebar renders with correct active-state highlighting on `/dashboard`, `/dashboard/all` (still highlights "Inbox" — prefix match working correctly), `/settings/account`, and `/settings/plan`; "Coming soon" items correctly non-interactive; full production build confirmed all four routes still resolve to their original URLs after the route-group move (`/dashboard`, `/dashboard/all`, `/settings/account`, `/settings/plan` — no path changes). Mobile hamburger/drawer behavior implemented with standard Tailwind `md:` breakpoints (well-established framework behavior) but not independently viewport-verified this session — the browser automation's `resize_window` tool didn't actually shrink the rendering viewport in this environment (`window.innerWidth` stayed at desktop size despite a "successfully resized" response). Full mobile QA is Step 38's explicit job anyway; flagging so it isn't assumed already proven.
    - All test artifacts cleaned up: test users (`mailpilot-step31-test@mailinator.com`, `mailpilot-step34-test@mailinator.com`) deleted, all 6 Postgres tables verified at 0 rows, throwaway scripts deleted.
  - **Step 32 done (2026-08-24): custom categories — the more involved step, deliberately built last.** Discussed the real technical shape with you directly before building: the classifier's `category` field is a Zod `z.enum(DEFAULT_CATEGORIES)` baked into the structured-output schema, so Claude is *constrained* to only ever return one of those exact values — there's no way to add a category by mentioning it in the prompt alone. Went with the simpler of two options discussed (a structured name+description form) over a Quell-style natural-language rule parser, since the latter is explicitly already planned as its own later phase (Phase 9 in `CLAUDE.md`) — decision + reasoning recorded there so Phase 9 builds on top of this mechanism instead of duplicating it.
    - `worker/src/classification/classify.ts` restructured: `buildSchemaAndPrompt(customCategories)` now builds the Zod schema and system prompt **per call**, unioning the 5 defaults with whatever categories this specific user has defined (loaded once per job in `backlog-worker.ts`'s new `loadCustomCategories`, not once per email). Zero custom categories → byte-identical schema/prompt to before this existed (verified by inspection, not assumed) — this is what keeps the existing `/evals` baseline meaningful.
    - `POST /api/settings/categories` (new) — creates a `rules` row (`rule_type: category_definition`, `rule_data: {name, description}`). Validated: name/description required and length-capped, can't collide with a default category name (would make the classifier's enum ambiguous), can't duplicate an existing custom category name, capped at 10 custom categories per user. `GET` lists them; `DELETE /api/settings/categories/[id]` soft-deletes (matches the existing rules pattern elsewhere — already-classified mail keeps its label, only future classification stops considering it).
    - `/settings/categories` (new page) — add-category form + list with Remove, added as a third tab alongside Account/Plan & usage in `settings-tabs.tsx`.
    - **Re-ran `/evals` per `CLAUDE.md`'s hard rule before considering this shippable — and got this wrong on the first pass, corrected after you pushed back.** First run: 96.2% priority / 96.2% category / 92.3% both-correct, with `transactional-flight-checkin-1` missing category (got `Notification`, expected `Transactional`). Initially reported this as "normal LLM run-to-run variance" — that was wrong, and I hadn't actually checked. When asked directly why the number wasn't higher and whether it had been fixed, re-ran the eval twice more: **same exact failure, identical reasoning, every time** — a consistent, reproducible miscategorization, not noise. Root-caused it to a real prompt ambiguity: the `Notification` category is defined as "anything automated that isn't a purchase/billing/**travel record**," but a flight check-in reminder *is* a travel record, so by the prompt's own stated logic it should already land in `Transactional` — the model just wasn't applying that boundary consistently (this exact Transactional/Notification boundary was already tightened once before, in Step 20; it had resurfaced). Fixed by making `Transactional`'s definition explicit about travel *reminders*, not just booking confirmations: "ANY travel-related automated message — booking confirmations, check-in reminders, boarding passes, itinerary/gate changes... even if it's phrased as a reminder/alert." Re-ran twice after the fix: **category accuracy back to 100% (26/26), both-correct 96.2% (25/26)**, stable across both runs — matching the original Step 20 baseline exactly. The one remaining miss (`tricky-human-but-low-stakes-1`) is the same fixture already documented in Step 20 as a deliberate safe-direction over-flag per the PRD's explicit bias, not a new issue. Confirmed the custom-category path (this step's actual new code) still classifies correctly after the prompt edit, same as before.
    - **Verified the actual new behavior with real Claude calls, not just the unchanged-baseline path:** a bank statement email classified as `Transactional` by default correctly reclassified to a custom `Finance` category once defined — and, just as importantly, an unrelated newsletter email stayed `Newsletter` in both cases, proving the custom category doesn't get over-applied to things that don't match it. Then verified the full UI round-trip live: added "Finance" via the real form, confirmed the `rules` row, removed it, confirmed `active: false` (not hard-deleted). All three input-validation guards (default-name collision, over-length name, missing description) verified via direct API calls, not just eyeballed in code.
    - **Fast-follow closed (2026-08-24): `/dashboard/all`'s filter pills and the category-correction dropdown now include custom categories, not just the 5 defaults.** `all/page.tsx` fetches the user's active `category_definition` rules server-side and passes the names down; `all-list.tsx`'s `FilterOption`/`matchesFilter`/`getDisplayBucket` were generalized from a fixed switch over the 5 defaults to fall through to a plain `item.category === filter` match for anything else — a custom category's display name is just whatever the user typed, no separate mapping needed the way `Human`→`Primary` needs one. `category-correction-select.tsx` appends the same names as extra `<option>`s.
      - **Real bug this surfaced, not shipped silently:** `POST /api/emails/[id]/correct`'s category validation only ever checked against `DEFAULT_CATEGORIES` — the dropdown would happily offer "Finance" as an option, but the server would have rejected it with a 400 "Invalid category". Fixed by validating against the union of defaults + this user's own active custom categories (fetched the same way `backlog-worker.ts` already does for classification, just on the web side).
      - **Verified directly, not just typechecked:** inserted a synthetic email + a real `Finance` custom-category rule via SQL for a real logged-in test user, then called the real `/correct` endpoint twice — `category: "Finance"` correctly passed validation (reached the next check in the route, an unrelated 409 for no Gmail connection on this synthetic test user — expected, not a bug), while `category: "TotallyBogusCategory"` was still correctly rejected with 400. Proves the fix accepts real custom categories without opening the door to arbitrary strings.
      - All test artifacts cleaned up: test user + synthetic rows deleted (cascade), all 6 tables verified at 0 rows.
    - All test artifacts cleaned up: test user `mailpilot-step32-test@mailinator.com` deleted, all 6 Postgres tables verified at 0 rows, throwaway eval/verification scripts deleted.
- **Phase 7 done criteria met.** `/settings/account`, `/settings/plan`, `/settings/categories` all match `docs/app-flow.md`'s page list; sidebar nav matches the spec (Inbox/Settings active, Commitments/Unsubscribe muted "Coming soon").
- [ ] Phase 8: UI/UX Redesign & Polish (Steps 36-39) — in progress
  - **Design direction agreed 2026-08-24** via an interactive HTML preview artifact — not a single up-front decision, an iterative review: palette, typography (Baloo 2 + Plus Jakarta Sans, matching the user's own login page), button motion, the "glow" effect, category colors, the sidebar, the Needs You card, Browse All, and Settings were each mocked up, reacted to, and revised in place before any real code was touched. Sourced from three things: the user's own `mailpilot-login_page.html` (aviation/pilot motif, warm sunset gradient, glassmorphic card, gradient-halo ring, paper-plane button animation), `page_highlight.png` (the specific glow effect to reuse), and Quell's dashboard screenshots (sidebar identity block, settings depth, a dark stat-card focal point).
  - **Real design decision, not just aesthetics — reserved the login page's full maximalism for the login page.** Confetti, flying planes, and the full animated sunset gradient are a marketing moment; a dashboard opened 20 times a day needs the same identity (palette, type, button feel) with real restraint, or "cheerful" becomes "distracting." This distinction was stated explicitly and confirmed with the user before any code was written.
  - **Category colors were reasoned through with the user across several corrections, not decided once:**
    - Primary → green (not gold, the initial draft) — real people, nothing urgent, "this is fine"; gold competed visually with coral's urgency.
    - Newsletters → gold (freed up once Primary became green) — a real, common category, correctly *not* the neutral/gray treatment (that belongs to "Other," the genuine catch-all).
    - Transactional → rose (not teal, the initial draft) — teal sat too close to green on the color wheel and was visually indistinguishable at badge size; rose is unambiguous.
    - "Mark as priority" → coral (same judgment as the Needs You badge — same axis). "Not priority" → **neutral slate, not green** — an early draft matched it to Primary's green on the theory that both mean "calm," but priority and category are independent axes (removing urgency doesn't make something Primary, it could stay any category), so borrowing a category color for a priority action was a real logic error, not just a style choice. Caught by the user re-flagging the collision twice.
    - The X "dismiss" icon's hover rotation was fixed twice: first `rotate(90deg)` was a no-op (an X has 4-fold rotational symmetry — rotating it 90° is visually identical to not rotating it at all, so the motion genuinely never showed). Fixed to `45deg`, which is visible — but 45° turns an X into a "+", which reads as "add," the opposite of what the button does. Final fix: a small 15° tilt + scale, which is visible without the icon ever becoming a different recognizable symbol.
  - **Built into the real app (2026-08-24):**
    - **Foundation** — `tailwind.config.ts` and `globals.css` retinted (coral/gold/violet/sky/rose/green, warm cream background, replacing the old indigo/violet system); root `layout.tsx` swapped Inter for Baloo 2 (headings/numbers/buttons only, never body text) + Plus Jakarta Sans. Because the whole app already used semantic Tailwind tokens (`bg-accent`, `text-text-secondary`, etc.) rather than hardcoded hex values, this one change cascaded correctly to every existing page automatically — confirmed live on `/settings/account`, which was never touched directly.
    - **Sidebar** (`(app)/sidebar.tsx`) — added a real identity block (avatar with initials + your name/email, per Quell) above the nav; `(app)/layout.tsx` now fetches the authenticated user server-side to supply it. Avatar color is a deterministic hash of the name, not random, so the same person always gets the same color. This hash/initials logic was extracted into `web/lib/avatarColor.ts` so the Needs You card could reuse it rather than duplicating it — a mistake (duplicated, drifting CSS) already made once in the preview artifact and deliberately not repeated in real code.
    - **Needs You card** (`priority-list.tsx`, `correction-button.tsx`, `undo-button.tsx`) — sender avatar, a real category badge (reusing the same reasoned colors, extended into `categoryDisplay.ts` as `CATEGORY_BADGE_CLASSES` — required adding `category` to the page's Supabase select query and the `PriorityListItem` type, which didn't carry it before), the glow ring applied to only the single top-sorted item (not every card — the whole point of the effect is signal, not decoration), and the reasoned button colors (coral "Mark as priority", neutral "Not priority", untouched neutral "Undo" since it's not on the priority axis).
    - **Verified against real, live Gmail data** (`karter.t112@gmail.com`), not just the empty state: a real 30-day backlog run classified 31 new emails (89 found, capped by the daily limit with 44 already used earlier that day) in **36.3 seconds** (database-timestamped, `triage_sessions.started_at`/`completed_at` — not a guess), 12 flagged priority — confirming the avatar/badge/glow/button treatment renders correctly against real sender names, categories, and reasons, not just the mockup's fabricated examples. A second real run earlier the same day: 44 emails in 64.1 seconds. Both land in the same ~1-1.5s/email range, the honest steady-state throughput number, independent of the one-time Next.js dev-mode route-compile costs (37-72s, seen once per route per server restart) that are unrelated to actual product performance.
    - **Real environment issue hit and fixed, not glossed over:** over the course of this session's many restarts, 10+ orphaned `tsx watch` worker processes had silently accumulated, all still running and competing for the same BullMQ queue — this was the actual cause of a ~5 minute delay the user rightly pushed back on, not the classification pipeline itself (which the timestamped numbers above prove is fast). Fixed by identifying and killing every stale process via `Get-CimInstance Win32_Process`, confirming exactly one clean instance of each server before continuing. Worth remembering for future sessions: check for accumulated zombie processes before assuming a slowdown is a code problem.
  - All test artifacts cleaned up: test users deleted, the real `MailPilot/Priority` label removed from all 23 labeled messages and deleted from the account, all 6 Postgres tables verified at 0 rows, throwaway scripts deleted.
  - **Login page rebuilt for full fidelity (2026-08-24/25).** First pass was a significant, uncorrected simplification of the source `mailpilot-login_page.html` — caught by the user in two escalating rounds of feedback, most specifically "paper envelope flying is missing." Re-read the original file line-by-line and rebuilt `web/app/login/page.tsx` to match it: `hero-plane`/`hero-plane-2`/`hero-envelope`/`hero-envelope-2` full-viewport flying elements, all 5 float-icons and all 4 sparkles (previously only 4 and 3), the full staggered `.reveal`/`riseIn` per-element entrance system (previously one single card-level animation), brand-badge wobble, headline underline draw-in + inline animated plane/flame/embers/nav-light, page- and card-level grain texture, and the confetti-burst + button-takeoff animation wired to a genuine Supabase auth success (not decorative). Kept the three explicitly agreed departures from the source: Remember me shown, Forgot password shown but inert (no href, not wired — no reset flow exists), Apple sign-in omitted (Google only).
    - **Real interaction bugs found and fixed after the visual rebuild, each caught by the user actually using the page, not by reading code:** the card was mouse-tilting jerkily while the ambient icons didn't move at all — root cause was exactly backwards from the source file: the icons' `--mx`/`--my` + per-element `--depth` parallax (CSS-transition-smoothed) had been dropped entirely, while the card's tilt was being set from raw mousemove events via React state plus a `transition` the original never had, fighting the entrance animation's `fill: both` hold. Fixed by porting the `--depth`-based parallax onto every blob/float-icon and removing the card tilt entirely (confirmed via the user's explicit "make the card fix[ed]") — the halo ring's continuous 14s rotation was the last residual motion and was frozen too, once the user flagged it as still "moving here and there."
    - **Legibility, not just fidelity:** "Forgot password?" and "New to MailPilot?" were exactly the same `#63709A` the source file itself uses — genuinely too low-contrast against this page's warm, colorful animated glass card despite matching the original. Iterated twice: first to a darker `#4B5A7C` (still not enough, per the user), then to the full `#1B2A4A` navy already used for the headline — same treatment applied to "Remember me" for consistency even though it wasn't separately flagged.
    - **The USP subtext ("...sorted, summarized, and stress-free.") got its own design round.** First attempt animated the text's own `background-clip: text` gradient fill — this made the sentence fully disappear at points in its cycle, caught during verification and reverted before shipping. Second attempt (a sliding shimmer band overlaid on always-solid text, same technique as the button) worked but the user didn't like the sliding motion itself. Presented 4 concrete alternatives as a live, looping HTML artifact (glow pulse / key-phrase color pulse / highlighter wash / no animation) rather than describing them in prose — user picked "key-phrase color pulse" (only "sorted, summarized, and stress-free" pulses coral; rest of the sentence stays static navy) from watching them side by side.
    - A real tooling incident during this work: a stray `npm run build` (run to sanity-check a compile error) wrote production build manifests into the same `.next` directory the running `next dev` server was using, corrupting it and breaking the live page with no code-level cause. Fixed by killing the dev server, deleting `.next`, and restarting clean — worth remembering: never run `next build` in the same directory as a live `next dev` process.
  - **Onboarding (Connect Gmail + Choose Backlog Range) brought onto the Phase 8 system (2026-08-24/25).** User asked for suggestions before any change, specifically whether a sidebar belonged on these screens — recommendation (accepted): no, onboarding is a focused linear wizard with nothing yet to navigate to, and a sidebar here would look like the main app before setup is actually done; used a small "Step 1 of 2"/"Step 2 of 2" dot indicator (`onboarding/step-indicator.tsx`) instead. Both screens got the glass-card/icon-badge/Baloo-headline treatment matching the login page's world but restrained, per the same interior-page principle as the dashboard. Reviewed as a live HTML artifact (toggle between both steps) before touching real code. Backlog range options gained per-option icons + a short sub-label ("Recommended — a full picture", etc.) and a proper selected-state checkmark, replacing plain bordered boxes.
  - **Backlog-scan UX overhaul — Option B (2026-08-25).** Two problems raised together, with suggestions requested before any change: (1) the post-save success message rendered under the unchanged "Choose your backlog range" heading, now stale/wrong; (2) the user is stuck on a static onboarding screen for however long the scan takes (measured elsewhere this session at ~1-1.5s/email — a 30-day backlog can genuinely run a couple of minutes), with no visibility into progress. Presented two real options — a blocking "Setting up your inbox..." wait screen (Option A) vs. dropping straight into the real dashboard with the sidebar while the scan finishes in the background (Option B) — recommended and the user chose B, since the worker already writes each email's classification to the DB as it's classified (not batched at the end), so the dashboard's Needs You list can genuinely fill in live in front of the user rather than showing a spinner.
    - New `backlog_jobs` table (migration `0010`, paired rollback) tracks whether a scan is in flight, deliberately separate from `triage_sessions` (whose "one row per *completed* run" contract stays untouched). Row inserted by `POST /api/onboarding/backlog` **at enqueue time** (not when the worker picks the job up) so the dashboard reflects the user's action immediately, not after a possible queue delay; `backlog-worker.ts` updates the latest `processing` row for that user to `completed` on success or `failed` (new `worker.on("failed", ...)` handling) on error.
    - `web/app/(app)/dashboard/scan-progress-banner.tsx` (new client component) polls `GET /api/onboarding/backlog/status` every 5s while a scan is active and calls `router.refresh()` on each tick — shared by both onboarding's first scan and the existing `CheckNowButton`'s manual re-checks, so both get the same live banner "for free." Dashboard's empty state now branches on `hasActiveBacklogJob` — "Scanning your inbox..." instead of the previously-always-wrong "You're all caught up" while a job is running.
    - `backlog-form.tsx` restructured so the heading/description live inside the same client component as the form's status, not split across a server-rendered heading it can't react to — success now shows a distinct "You're all set" confirmation (checkmark, no mention of "choose") for ~1.4s, then `router.push("/dashboard")`, directly fixing the stale-heading bug rather than papering over it with more copy.
    - **Verified end-to-end at the DB level, not just by inspection:** a throwaway auth user (created via `supabase.auth.admin.createUser`, matching this project's established test-user convention) exercised the real lifecycle — insert as `processing` → active lookup finds it → update to `completed` → active lookup correctly returns null → user deletion cascades `backlog_jobs` clean. Both `web` and `worker` typecheck clean. Live authenticated screenshots of the actual onboarding/dashboard flow were **not** taken this round — both routes are auth-gated and stand up a real Gmail-connected session, which wasn't available in this environment; flagging so this isn't assumed already visually proven the way the dashboard/login work earlier in Phase 8 was.
  - **Dashboard rebuilt rows-only + Browse All restyled (2026-08-26).** Extended, multi-round iteration with the user, mostly preview-artifact-first (dashboard-at-a-glance mockup, category-rows-preview, dashboard-summary-preview, check-now-button-options, filter-pills-preview — all published and reviewed before real code changed) rather than editing live and hoping.
    - **Dashboard restructured around "at a glance," not a second place to handle mail.** The old tile grid (5 small boxes, cramped once action subtitles + sender previews were added) became a vertical stack of full-width Tame-style rows (icon, title, action subtitle, "From: X, Y, +N more" sender preview, count, chevron) — each a real `Link` into `/dashboard/all?filter=...`, not just decorative. The "Other"/uncategorized bucket is deliberately excluded from these rows (`GLANCE_CATEGORY_VALUES` in `categoryDisplay.ts`) — showing it as a tile would present classifier uncertainty as if it were a meaningful, actionable category; it's still a real filter tab on `/dashboard/all`, where it's genuinely useful for finding-and-correcting misclassified mail.
    - Once rows linked out to the real list, the dashboard's own inline "Needs You" email list became pure duplication — removed entirely, along with the now-orphaned `priority-list.tsx` (which also retired, by deletion, an already-flagged-but-unfixed bug: rotating a wide/short rectangle 360° for the old glow-ring effect ballooned its bounding box well outside the card). `session-summary.tsx` ("N triaged · N flagged... Last scan: ...") was also cut, superseded first by a narrative sentence and then, after the user pushed back that a fully numeric page "looks like a simple mailbox, nothing AI related to it" (comparing against Quell's first-person "I filed 19 newsletters..." copy), replaced with a short first-person line in `greeting-banner.tsx` ("I went through N new emails while you were away...") — proving the AI did something, not just restating counts the rows already show.
    - `/dashboard/all`'s filter pills now carry their category's color even at rest (a soft tint, reusing `CATEGORY_BADGE_CLASSES`'s existing color map — nothing new invented) and switch to a solid fill when active, each with its own count (free — computed from data already loaded for filtering). Sender names are now run through `displayNameFromHeader` (this page had been showing raw `"Name <email>"` headers unprocessed the whole time — a real bug, not a style gap) and get avatar circles matching the dashboard's own list. The back-link was renamed "Back to Needs You" → "Back to Inbox," matching the sidebar's own name for that page now that `/dashboard` isn't needs-you-only.
    - **Real bug found and fixed, confirmed live by the user, not just in theory:** "Open in Gmail" links were opening the wrong Google account's inbox for anyone signed into multiple Google accounts in their browser — the hardcoded `/u/0/` in the URL path always pointed at the browser's *first* signed-in account, not necessarily the connected one. First fix attempt (adding `?authuser=<email>` while *keeping* `/u/0/`) did not work — user confirmed still wrong account. Root cause on the second pass: Google's routing honors the path segment over the query string when both are present. Fixed by dropping `/u/0/` entirely and resolving purely via `authuser=<connected gmail address>` (fetched from `gmail_tokens`, threaded through as a new prop). Confirmed fixed by the user on the second attempt.
    - The category-correction dropdown (shows/edits an email's real category) was visually near-identical to the "Needs You" bucket badge next to it — read as a second, contradictory category label. Prefixed with "Category:" and given a less-rounded shape so it reads as a control, not a badge.
    - **Classification rubric tightened, with `/evals` run before and after per the hard rule.** User questioned whether routine automated emails (a payment-decline notice, an account-verification email) belonged in "Needs You" and asked whether the classification process itself was sound. Checked the actual prompt and eval fixtures rather than guessing: both cases were *already* correctly classified per the existing, deliberate rubric ("automated messages with a real deadline" explicitly includes payment failures and expiring verification codes) and covered by existing passing eval fixtures — not a process failure. Separately, formed the opinion that the rubric itself was too inclusive (any automated deadline, however trivial), diluting the "Needs You" signal's trustworthiness. Added 3 new eval fixtures targeting exactly this gray area (a routine "confirm your email" with no stated expiry/consequence, the same but with a real 24h expiry, and a login code with no stated expiry but implicit urgency) — baseline run against the *unmodified* prompt confirmed the routine-confirmation case failing (27/29, 93.1%) as suspected, then tightened `classify.ts`'s rubric to require a genuine cost to waiting, not just "automated with any deadline." Re-ran: **28/29 (96.6%), zero regressions** — the one remaining failure is the same pre-existing low-stakes-human-question over-flag documented back in Step 20, unrelated to this change.
    - **Operational gotcha, not a code bug:** "Last checked" was showing hours-stale because the `worker` process (which hosts the hourly poll scheduler) simply wasn't running in this dev session — only `web`'s dev server was up. Started it; it immediately caught up on a queued backlog job that had been waiting the whole time. Worth checking first next time a "why isn't this updating" question comes up in dev.
    - Also hit and fixed, unrelated to the redesign itself: three separate stale `npm run dev` processes (one running since a prior session) were all competing for port 3000, which was the actual cause of a reported 404 — killed all three (full process trees via their real Windows PIDs, not the POSIX-emulated ones Git Bash's `ps` shows), cleared `.next`, restarted clean.
    - Committed in three pieces (`354f685` dashboard rows-only + All Mail restyle, `a0ed2ea` Gmail-link fix + category-control clarity + filter pill colors/counts, `d4bf802` classification rubric + evals) and pushed to `origin/main`.
    - **Not yet visually verified with a real authenticated session by me** — same limitation as the rest of this session: no way to sign in as the user directly, so every fix here was validated via `tsc`/dev-server-compile checks plus the user's own live screenshots and confirmations in chat, not a first-party screenshot walkthrough.
  - **Remaining for Phase 8:** the 3 Settings pages (vertical sub-nav instead of the tab strip, styled Plan/Categories content). Steps 37-39 (contrast verification, mobile pass, empty/error states) still follow once the visual redesign itself is complete.
- [ ] Phase 9: Testing (Steps 40-43) — not started
- [ ] Phase 10: Deployment (Steps 44-47) — not started

## Open Issues / Blockers
- **No privacy policy exists yet.** Surfaced (2026-08-22) while researching how competitor "Tame My Inbox" discloses AI/Gmail data handling — their policy includes a required **"Google API Services — Limited Use Disclosure"** section, which Google mandates for any app requesting sensitive scopes (`gmail.readonly`/`gmail.modify`, exactly what MailPilot uses) as part of OAuth verification. This is likely a hard prerequisite for the Google verification review below, not just a nice-to-have — needs to exist before that submission, though not urgent today. Decision made the same day: MailPilot will send full email body content to Claude for classification (not just snippets, since the PRD's "never miss something urgent" requirement needs real signal), never stored in our own DB, relying on Anthropic's stated API terms (no training on API data, limited retention) — same trust model Tame itself uses with the same provider. Confirmed MailPilot's zero-storage rule is stricter than Tame's actual practice (Tame caches content server-side 24-48h; MailPilot never caches at all) — deliberately keeping the stricter rule.
- Google OAuth verification review (needed for `gmail.modify` scope) has not been started yet. Per `docs/trd.md` and `docs/implementation-plan.md`, this should begin as soon as the OAuth consent screen is finalized (Phase 3) — it's a slow external Google review process, not a code task, and gates real-user launch in Phase 10.
- Supabase DB password and API keys were rotated once (2026-08-21) after being inadvertently surfaced in a chat session during setup. `web/.env.local` holds the current, rotated values.
- **Redis public access before production:** Railway Redis is currently reachable from the public internet (password-protected TCP proxy), chosen for local-dev simplicity over the more secure CLI-tunnel option. Should be revisited before Phase 10 (production deployment) — either switch to the CLI-tunnel approach or otherwise lock down access.
- **Design direction decided (2026-08-22), rollout incomplete:** the earlier "cheerful/3D login page" ask escalated into a full brief revision — `docs/ui-ux-brief.md` now specifies the colorful/motion-forward direction app-wide (see Phase 4 note above), not just for login. `/login` itself and the Step 11-13 placeholder pages (`/dashboard`, old parts of `/onboarding/*`) still visually reflect the *old* restrained direction and haven't been updated yet — worth doing a pass to bring them in line, rather than letting the app have two different visual languages side by side. Not urgent/blocking, but shouldn't be forgotten either.

## Notes for Next Session
- **Saved for easy revert (2026-08-26): the current two-axis dashboard/glance model, as of commit `b619b35`.** Category (`Human`/`Notification`/`Newsletter`/`Transactional`) and the priority flag (`priority_flagged`, shown as "Needs Your Attention") are independent axes today — an email's category count includes it whether or not it's also flagged priority, so the glance-row counts *don't* sum to the account total (by design, not a bug) and "Needs Your Attention" overlaps with the category rows rather than being mutually exclusive with them. Under discussion (2026-08-26): switching to a Tame-style single-axis model instead, where a priority-flagged email counts *only* under Needs Your Attention and is excluded from its category's row/count/filter — mutually exclusive buckets that sum cleanly, matching Tame's actual behavior (confirmed: their own numbers, 6+11+6+27=50, do sum to their total, unlike ours). If that's built and ever needs reverting: the count queries in `dashboard/page.tsx` and `lib/categorySenderPreviews.ts`, and the category cases in `dashboard/all/all-list.tsx`'s `matchesFilter`, would need the `priority_flagged = false` exclusion removed again to restore today's overlapping behavior — `git show b619b35:web/app/(app)/dashboard/page.tsx` (etc.) has the exact prior version of each file.
- **UI/UX redesign timing decided (2026-08-24), not yet started.** The user asked for a genuine visual/UX overhaul (referencing competitor Quell's UI — single restrained accent color, cream background, avatar circles, bucketed sections, clear primary/secondary button hierarchy, a persistent sidebar — see the updated [[project_beat_tame_standing_goal]]-style comparison this session) plus their own custom login page + color palette (still not yet shared — see `project_ui_redesign_deferred` memory). Rather than start now or push it all the way to the very end, the plan is to do it **after Phase 7 (Settings) finishes, in place of/expanding the existing "Phase 8: UI Polish" slot, before Phase 9 (Testing) starts** — reasoning: every page in the app exists once Phase 7 is done, so the redesign touches each page exactly once instead of being redone; and it needs to land before Playwright E2E tests are written (Step 41) so those tests aren't written against a UI that then changes underneath them. `docs/implementation-plan.md`'s Phase 8 section updated to reflect the expanded scope. Do not start this work early without the user explicitly asking.
- Migrations live in `/supabase/migrations` as paired up/rollback SQL files (e.g. `0001_create_users.sql` + `0001_create_users_rollback.sql`), applied via `npm run migrate` / `npm run migrate:down` in `/web` (script: `web/scripts/migrate.mjs`), using `DATABASE_URL` from `web/.env.local`.
- `/shared` is wired into `/web` and `/worker` as a local `file:` dependency (`"shared": "file:../shared"` in each `package.json`) — currently just a placeholder type-only import (`Placeholder`), verified resolvable in both. Real shared types (the classification schema) land in Phase 5.
- `web/.env.local` holds real Supabase credentials and is gitignored (confirmed via `git check-ignore`, both root and `web/.gitignore` cascade correctly). If `DATABASE_URL`'s password contains special characters, they must be percent-encoded (`@`→`%40`, `%`→`%25`, `^`→`%5E`, `&`→`%26`, `(`→`%28`, `$`→`%24`, `)`→`%29`) or the connection string won't parse.
- Working pattern established this project: every step gets explained before starting, executed, verified (often with a throwaway test script that's deleted afterward), then committed locally — pushed to GitHub only after explicit go-ahead. Destructive actions (e.g. `migrate:down` against the real DB) are confirmed with the user first, even when low-risk.
- `migrate.mjs` has no applied-migrations tracking — every `npm run migrate` re-runs all up-files from scratch, so each one must stay idempotent (`CREATE TABLE IF NOT EXISTS`, `DROP POLICY IF EXISTS` before `CREATE POLICY`, etc.) or a second run will break. `migrate:down` rolls back *everything* in reverse, including table drops — to test/undo a single migration in isolation, run that one file directly instead (see how the 0006 RLS rollback was tested).
- Important gotcha for any future RLS testing: the `DATABASE_URL` connection (via `pg`, used by `migrate.mjs`) runs as the `postgres` role, which **bypasses RLS entirely** — it's the right tool for schema changes, the wrong tool for proving RLS actually blocks anything. Real RLS tests need the `anon` key + a real authenticated session (`supabase.auth.signInWithPassword`), the same access path the real app uses.
- Claude Code plugins installed: `typescript-lsp`, `pr-review-toolkit`, `supabase` (MCP, OAuth-authenticated — broad org-level scope: `projects:write`, `database:write`, `secrets:read`, etc.), and `github` (MCP — required installing the `gh` CLI via winget first, then `gh auth login`). Standing preference: never invoke a newly available plugin tool/agent without asking first, even after it's installed/authenticated — see memory `feedback_ask_before_new_plugin_tools`. In practice, prefer the narrowest-scoped credential for a task (e.g. project-scoped `DATABASE_URL`/anon key over the account-wide Supabase MCP) unless there's a specific reason to reach for the broader one.
- Guardrail hooks are in `.claude/settings.json` + `.claude/hooks/` (protect-paths.mjs, lint-typecheck.mjs) — but hook config is snapshotted at Claude Code session startup, so changes made mid-session won't govern that same session's own tool calls. A fresh session is needed for them to actually take effect live (both were unit-tested directly and work correctly in isolation, independent of this caveat).
- `web/.env.local`'s Supabase URL/anon key are now `NEXT_PUBLIC_SUPABASE_URL` / `NEXT_PUBLIC_SUPABASE_ANON_KEY` (renamed from the unprefixed versions used in Steps 5-9) — Next.js only exposes `NEXT_PUBLIC_`-prefixed vars to browser code, needed for `lib/supabase/client.ts`. `SUPABASE_SERVICE_ROLE_KEY` and `DATABASE_URL` stay unprefixed on purpose (must never reach the browser). `.env.example` updated to match.
- Windows gotcha hit during Step 11 testing: stopping a `npm run dev` background task (via `TaskStop`) doesn't always kill the underlying `node.exe` child process — it can stay bound to its port after the wrapper task is gone. This caused a confusing "unstyled page" bug that was actually a browser hitting a stale orphaned server on an old port, not a real CSS issue. If a dev server behaves unexpectedly after being "stopped," check `Get-NetTCPConnection -LocalPort 3000,3001,... -State Listen` for leftover `node.exe` processes before assuming it's a code bug.
- Brand design tokens from `docs/ui-ux-brief.md` are now wired into `web/tailwind.config.ts` (colors: `accent`/`accent-hover`, `surface`, `text-secondary`, `border`, `error`; font: Inter via `next/font/google`) and `web/app/globals.css` (cream background, light-mode-only per the brief — no dark mode media query). Reuse these tokens (not raw hex/arbitrary values) in every future page.
- `web/scripts/show-encryption-demo.mjs` was deliberately left in place (not deleted like other throwaway scripts) — you asked to circle back and run it yourself to see the Gmail token encryption/decryption side by side. It'll report "no gmail_tokens row found" until Gmail is connected again (table is currently empty after Step 13's testing cleanup).
- `/shared`'s exports are now a mix of type-only (`Placeholder`) and real runtime values (`BACKLOG_QUEUE_NAME`, etc.) — any *new* runtime export needs `shared/package.json`'s `"main"` field and `web/next.config.mjs`'s `transpilePackages: ["shared"]` (both already in place) to actually resolve at build time; type-only imports don't need either, since webpack strips them entirely.
- claude-in-chrome browser automation gotcha hit repeatedly this session: the `computer` tool's screenshot action can get into a broken state on a tab that's been open a while (`Page.captureScreenshot` errors, "0x0 viewport" errors) — opening a **fresh tab** and re-navigating reliably fixes it, no need to debug further. Separately, the `find`-and-click pattern doesn't always reliably trigger React/Framer Motion event handlers on interactive elements — when a click via `find`+`computer` doesn't seem to register (e.g. UI state not updating), falling back to `javascript_tool` with a direct DOM `.click()` call is more reliable.
